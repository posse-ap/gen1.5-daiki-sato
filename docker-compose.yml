# version: '3'
# services:
#   web:
#     image: php:7.4.15-apache
#     container_name: quizy
#     ports:
#       - 80:80
#     volumes:
#       - .:/var/www/html
#     tty: true










version: '3'
services:
  web:
    container_name: quizy_nginx
    image: php:7.4.15-apache
    ports:
      - "80:80"
    depends_on:
      - app
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - .:/var/www/html
  app:
    container_name: quizy_php
    build: ./php
    image: php:7.4-fpm
    depends_on:
      - db
    volumes:
      - .:/var/www/html
  # db:
  #   container_name: quizy_db
  #   image: mysql:latest
  #   command: mysqld --character-set-server=utf8 --collation-server=utf8_unicode_ci
  #   environment:
  #     MYSQL_DATABASE: quizy
  #     MYSQL_USER: posse_user
  #     MYSQL_PASSWORD: password
  #     MYSQL_ROOT_PASSWORD: password
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - ./mysql/sql:/docker-entrypoint-initdb.d
  #     - ./mysql/data:/var/lib/mysql
  #     - ./mysql/my.cnf:/etc/mysql/conf.d/my.cnf
  ⋮
  db:
    image: mysql:5.7.19
    env_file: .env
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
      - ./db/initial.sql:/docker-entrypoint-initdb.d/initial.sql

volumes:
  db-data:
